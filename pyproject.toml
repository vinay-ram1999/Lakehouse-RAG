[project]
name = "lakehouse-rag"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "databricks-sdk>=0.67.0",
    "python-dotenv>=1.1.1",
]
